---
import { getCollection } from "astro:content";
import { withBase } from "../lib/paths.js";
import { trimText } from "../lib/content-display.js";
import AquariumBackdrop from "../components/AquariumBackdrop.astro";
import AquariumBubbles from "../components/AquariumBubbles.astro";
import ImageLightbox from "../components/ImageLightbox.astro";
import "../styles/site.css";

const siteName = "Aquatic Treasures of Fort Myers";
const siteEntry = (await getCollection("site"))[0] ?? null;
const siteContent = siteEntry?.data ?? {};

const {
  title = siteName,
  description = "Freshwater fish care sheets and aquarium resources from Aquatic Treasures of Fort Myers.",
  noIndex = false,
} = Astro.props;

const pageTitle = title === siteName ? title : `${title} | ${siteName}`;
const brandName = trimText(siteContent.siteName) || siteName;
const currentYear = new Date().getFullYear();
const currentPath = Astro.url.pathname;
const homePath = withBase();
const fishPath = withBase("fish/");
const notesPath = withBase("notes/");
const isHomePath = currentPath === homePath;
const isFishPath = currentPath === fishPath || currentPath.startsWith(fishPath);
const isNotesPath = currentPath === notesPath || currentPath.startsWith(notesPath);
const navItems = [
  {
    href: withBase("fish/"),
    label: "Care Sheet Library",
    current: isFishPath ? "page" : undefined,
  },
  {
    href: withBase("notes/"),
    label: "Resources",
    current: isNotesPath ? "page" : undefined,
  },
  {
    href: withBase("#about"),
    label: "About",
    current: isHomePath ? "location" : undefined,
  },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    {noIndex && <meta name="robots" content="noindex, nofollow" />}
    <meta name="google-site-verification" content="Dty26rHGTcdvTMSIKZot6QqYz_D-mByo-XhJog2CNU8" />
    <title>{pageTitle}</title>
    <link rel="icon" type="image/png" sizes="64x64" href={withBase("favicon-octopus.png")} />
  </head>
  <body>
    <AquariumBackdrop />
    <AquariumBubbles
      enabled={siteContent.bubbleBackgroundEnabled}
      density={siteContent.bubbleBackgroundDensity}
      maximumOpacity={siteContent.bubbleBackgroundMaximumOpacity}
      speed={siteContent.bubbleBackgroundSpeed}
    />
    <header class="site-header">
      <a class="site-brand" href={withBase()}>
        <span class="site-brand-mark" aria-hidden="true">
          <img src={withBase("brand-mark.webp")} alt="" />
        </span>
        <span class="site-brand-name">{brandName}</span>
      </a>
      <nav class="site-nav site-nav-desktop" aria-label="Main navigation">
        {navItems.map((item) => (
          <a href={item.href} aria-current={item.current}>
            {item.label}
          </a>
        ))}
      </nav>
      <details class="site-menu">
        <summary class="site-menu-toggle">
          <span class="site-menu-toggle-label">Menu</span>
          <span class="site-menu-toggle-icon" aria-hidden="true">
            <span></span>
            <span></span>
            <span></span>
          </span>
        </summary>
        <nav class="site-nav site-nav-mobile" aria-label="Main navigation">
          {navItems.map((item) => (
            <a href={item.href} aria-current={item.current}>
              {item.label}
            </a>
          ))}
        </nav>
      </details>
    </header>
    <slot />
    <ImageLightbox />
    <footer class="site-footer">
      <p>
        © {currentYear} {brandName} · Designed by Juxtaposition Sound &amp; Vision
      </p>
    </footer>
  </body>
</html>
